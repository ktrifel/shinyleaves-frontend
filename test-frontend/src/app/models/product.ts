// src/app/models/product.ts
export interface Product {
  p_id: number;
  name: string;
  price: number;
  slug: string;
  genetic: string;  // neu
  thc: number;      // neu
  cbd: number;      // neu
  effect: string;   // neu
}
